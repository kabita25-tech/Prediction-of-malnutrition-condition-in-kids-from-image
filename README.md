# Prediction-of-malnutrition-condition-in-kids-from-image
This project is an image classification model using a Convolutional Neural Network (CNN), which is trained with TensorFlow/ keras .The model's performance is measured by it's accuracy, loss and a confusion matrix to distinguish between different kids images 
